# prodweb

```
sudo apt-get install -y python3 python3-dev python3-venv build-essential git
git clone https://github.com/cstrutton/prodweb.git
cd prodweb
python3 -m venv venv
. venv/bin/activate
pip install wheel
pip install -r requirements.txt
```
